#run systemd as user instance
exec /usr/lib/sytemd/systemd --user &

#Keyboard Map
setxkbmap lv && xmodmap ~/Downloads/neo_de.xmodmap && xset -r 51
#setxkbmap de -variant nodeadkeys

#Pulseaudio
pulseaudio --start

exec startfluxbox

#case $1 in
#dwm)
#        exec dwm 
#        ;;
#fluxbox)
#        exec startfluxbox
#        ;;
#esac
